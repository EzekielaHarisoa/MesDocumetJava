* les interface sont pardefault en abstract meme leur methode
* ne support pas les attribut en  private et protected
* on peut utiliser >default methode() pour ne pas implementer dans les fils et considerer comme valeur par defaut mais on peut le redefinir et modifier
* @FunctionalInterface: utiliser pour une interface a un seurl methode dedans  