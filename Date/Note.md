# LocalDate
  .now()
  .EPOCH
  .of(2002,22,01) : pour verifier , pour rechercher , une codition

  LocalDate somedaty = LocalDate.of(2020-26-55)
  ssomeday.getDay()
          .getMounth() : getMounthValue()
          .getYears()